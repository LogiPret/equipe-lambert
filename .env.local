# Development environment variables - separate from production
# Copy your existing .env but change the database URL

# Use a different database name or connection string
DATABASE_URI=mongodb://localhost:27017/equipe-lambert-dev
# OR if using PostgreSQL:
# DATABASE_URI=postgresql://user:password@localhost:5432/equipe_lambert_dev

# Keep other environment variables the same
PAYLOAD_SECRET=your-payload-secret-here
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Supabase (can use same project or create a dev project)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
